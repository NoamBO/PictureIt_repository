<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <NumberPicker
        android:id="@+id/number_picker_dialog_wheel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_margin="20dp"
        android:divider="@color/app_most_common_yellow_color" />

    <Button
        android:id="@+id/number_picker_dialog_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/number_picker_dialog_wheel"
        android:layout_marginLeft="16dp"
        android:layout_toRightOf="@+id/number_picker_dialog_wheel"
        android:text="Cancel" />

    <Button
        android:id="@+id/number_picker_dialog_ok"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/number_picker_dialog_cancel"
        android:layout_alignBottom="@+id/number_picker_dialog_cancel"
        android:layout_marginRight="16dp"
        android:layout_toLeftOf="@+id/number_picker_dialog_wheel"
        android:text="Set" />

</RelativeLayout>